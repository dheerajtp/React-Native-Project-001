import { SafeAreaView, StatusBar, View, Text } from "react-native";

const App = () => (
  <SafeAreaView>
    <StatusBar />
    <View style={{ padding: 10 }}>
      <Text>Hitesh Choudhary React Native Tutorial Chapter One</Text>
    </View>
  </SafeAreaView>
);

export default App;
